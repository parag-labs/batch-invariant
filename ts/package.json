{
  "name": "@parag-labs/batch-invariant",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Batch-invariant reduction kernels: a row's output must not depend on who else is in the batch.",
  "scripts": {
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
